/**
 * Converts years to months.
 *
 * @param {number} years - The number of years.
 * @returns {number} The number of months.
 */
export function convertYearsToMonths(years: number): number {
  return years * 12
}
